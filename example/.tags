# Each line here defines a tag
# When installing modules by tag, `pont` will search for the presence
# of the tags for each module. Tags are referenced with a : prefix
# A dependency of `:font` means every module that is tagged as `font`
base
gui
tui
